import { Maid } from '@quenty/maid';

export namespace NetworkRopeUtils {
  function hintSharedMechanism(part0: BasePart, part1: BasePart): Maid;
  function clearNetworkOwnerHints(part: BasePart): void;
}
