import { InputModeType } from './InputModeType';

export namespace InputModeTypes {
  const Keypad: InputModeType;
  const Keyboard: InputModeType;
  const ArrowKeys: InputModeType;
  const WASD: InputModeType;
  const Mouse: InputModeType;
  const KeyboardAndMouse: InputModeType;
  const Touch: InputModeType;
  const DPad: InputModeType;
  const Thumbsticks: InputModeType;
  const Gamepads: InputModeType;
}
