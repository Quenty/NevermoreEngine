export namespace ScrollingDirectionUtils {
  function canScrollHorizontal(
    scrollingDirection: Enum.ScrollingDirection
  ): boolean;
  function canScrollVertical(
    scrollingDirection: Enum.ScrollingDirection
  ): boolean;
}
