# This file specifies selene linting rules
std = "roblox+testez"
exclude = ["**/node_modules/**", "**/Jecs.luau", "**/Iris.lua", "**/iris_*.lua"]

[rules]
if_same_then_else = "allow" # This actually makes code less clear sometimes
mixed_table = "allow" # Blend uses mixed tables