/**
 * Shared type definitions for studio-bridge CLI commands.
 */

/**
 * Minimal command definition metadata used by the barrel export pattern.
 */
export interface CommandDefinition {
  name: string;
  description: string;
}
