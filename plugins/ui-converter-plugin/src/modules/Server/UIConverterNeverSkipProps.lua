--[=[
	Properties we want to always have even if it's the default, for easier editing.
	@class UIConverterNeverSkipProps
]=]

return {
	UIScale = {
		Scale = true;
	};
	UICorner = {
		CornerRadius = true;
	};
	UIAspectRatioConstraint = {
		AspectRatio = true;
	};
}